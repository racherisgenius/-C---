1. if的常见问题解析

// 1.空语句的问题
/*
 if (3 > 2);
 等价于
 if (3 > 2)
       ;//这是一个空语句

*/
# include <stdio.h>
int main(void)
{
  if(1 > 2)      ；
            //这是空语句
  /*
    如果 1 > 2 那么直接运行后面的空语句。
    所以在这里就是三个语句了
  
  */
      printf("AAAA\n");
  printf("BBBB\n");
  //这里是两个语句哦！（不加if后面的分号）
  return 0;
}
