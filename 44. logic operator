int i = 10;
int j = 20;
int m; 
m = (1>2) ||(k=5);
prinf("m = %d, k = %d\n", m, k);
return 0;
在以上代码里，m=1，k=5。
 因为首先代码会判断1是否大于2，那么不对就往下一个条件执行。下一个条件是赋值，赋值完毕后，条件2就是true，那么整体为true
 那么m = 1因为true，k = 5 因为被赋值了
