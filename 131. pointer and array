2. 指针和数组
  1） 指针和一维数组
    一维数组名
      - 一维数组是一个指针常量；a = b；//这个是不对的因为不能改变数组的值
      - 他存放的是一个一维数组第一个元素的地址
    下标和指针的关系
      - 如果p是个指针变量，则
       p[i] == *(p+1);
    指针变量的运算
 ex：
 # include <stdio.h>
 int main(void)
 {
    int a[5];
    int a[3][4];
    return 0;

}
