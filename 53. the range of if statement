# include <studio.h>
int main(void)
{
  if(1>2)
     printf("AAAAA\n");
     printf("BBBB\n");
     return 0;
}
/* 在 xx 中输出结果是：
-----------------
BBBB
-----------------
    总结：
         if默认的只能控制一个语句的执行或者不执行
*/

2. if的范围问题
    1.
       if（表达式）
           语句A；
           语句B；
       解释：if默认只能控制语句A的执行和不执行
            if不能控制语句B的执行或者不执行
            或者说：语句B一定会执行。
     2. 
       if（表达式）
       {   
           语句A；
           语句B；
       }
       此时if可以控制A和B；
     由此可见：if默认只能控制一个的执行和不执行
              如果想控制多个语句，就需要用{}。
