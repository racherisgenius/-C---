*指针的作用：返回一个以上的值
# include <stdio.h>
f(int i, int j)
{
//你在这里不管怎么动i 和 j就是无法改变a 和b的值，因为他们本来就不是一个东西
}
void g(int * p, int * q)
{
   *p = 1;
   *q = 2;
}
int main(void)
{

  int a = 3, b = 5;
  
  g(&a, &b);
  printf("%d %d\n", a,b);
  f(a ,b); //如果你不发送地址的话，形参的值永远不会改变
  return 0;
}



**如何通过被动函数修改主调函数的值**
1. 实参要是为该普通变量的地址
2. 形参必须为指针变量
3. 在被动函数中可以通过 （*形参名 = ...）的方式就可以修改主调函数的值
