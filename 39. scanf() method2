1. scanf()用法二：
  ex：
  #include <studio.h>
  int main(void)
  {
     int i;
     scanf("m%d", &i); //希望用户输入一个m先，再输入一串别的整型。
     /*
      1.如果是m123n呢？
      -  结果就是i = 123 因为系统认为n不是个数字所以自动省略了。
     */
     printf("i = %d\n", i);
     return 0;
  }
   
   1）用法二： scanf("非输入控制符 输入控制符");
      功能： 非输入控制符必须原样输入。
