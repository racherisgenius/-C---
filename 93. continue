1. continue
- 用于跳过本次循环余下的语句，
- 转去判断是否需要执行下次的循环。
- 也可以用来判断是否有垃圾字符

ex:
for (1; 2; 3)
{
 A;
 B;
 continue;
 //如果执行该语句，该语句执行完后，CD会被忽略，直接执行3
 C;
 D;
}

while（表达式）
{
  A;
  B;
  continue;
  //如果执行该语句，该语句执行完后，CD会被忽略，直接执行表达式
  C;
  D;
}
